@page
@model XmlVisualizer.Pages.Club.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Club[0].Club_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Club[0].Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Club[0].League)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Club) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Club_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.League)
        </td>
        <td>
            <a asp-page="./Edit" asp-route-id="@item.Club_id">Edit</a> |
            <a asp-page="./Details" asp-route-id="@item.Club_id">Details</a> |
            <a asp-page="./Delete" asp-route-id="@item.Club_id">Delete</a>
        </td>
    </tr>
}
    </tbody>
</table>
